const myTrueVariable = true;
const myFalseVariable = false;

console.log(
    'myTrueVariable:',
    typeof myTrueVariable,
    myTrueVariable
);

console.log(
    'myFalseVariable:',
    typeof myFalseVariable,
    myFalseVariable
);

if (3 > 2) { // if (statement) is true, run block
    console.log('Three is greater than two.');
}
