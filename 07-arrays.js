// Array is a list of values
// "Indexed Array"
const myArray = [
    'Fish',    // 0
    'Giraffe', // 1
    'Dog',     // 2
    'Cat'      // 3
];

console.log(
    'typeof myArray:',
    Array.isArray(myArray), // true // typeof myArray, // "object"
    myArray // [ 'Fish', 'Giraffe', 'Dog', 'Cat' ]
);

console.log('myArray[1]:', myArray[1]);
